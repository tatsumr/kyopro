# Interval Set の実装の解説

## insert
`insert(l, r, x)` のアルゴリズムを解説する。

### 1. スタート地点を探す
左端が `l` 以下の区間のうち、最も右にあるものを探す。要は「 `[l, r)` と被っているかもしれない区間のうち、最も左にあるもの」である。実際は区間そのものではなくイテレータを取得する。このスタート地点となるイテレータを `it` とする。
```cpp
auto it = prev(s.upper_bound({l, SINF, e}));
```

### 2.1. `it` が `[l, r)` を完全に含んでいるとき
```
it         [L---------------------R)
挿入したい        [l-----r)
```
- 両者の値が同じ場合は、何もしなくてよい。このまま **3** の調整へ移る。
- 異なる場合は一旦 `it` を削除し、`[L, l)`、`[l, r)`、`[r, R)` を挿入する。その後 **3** へ。
  - （注意）区間を挿入するのはそれぞれ $L < l$、 $l < r$ 、 $r < R$ の場合。

### 2.2. `it` が `[l, r)` を完全に含んでいないとき
次のような方針で処理する。
1. 区間の左端を調整する。
2. `[l, r)` が完全に含んでいる区間をひたすら消す。
3. 区間の右端を調整する。
4.  `[l, r)` を挿入する。

#### 2.2.1. 左端の調整
##### 2.2.1.1. `it` と `[l, r)` が被っているとき
`it` が現在指している区間の値と `[l, r)` の値が同じかどうかに応じて適切な処理をする。
`it` と `[l, r)` の値が同じときは、
